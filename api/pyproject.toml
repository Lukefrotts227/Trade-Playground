[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ljf227", email = "ljf109@rutgers.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "google-genai>=2.18.1",
    "groq>=1.6.0",
    "pydantic-settings>=2.15.0",
    "uvicorn>=0.52.3",
]

[project.scripts]
api = "api:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
